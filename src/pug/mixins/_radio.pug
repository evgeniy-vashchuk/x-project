//- +radio({id: "subscribe", name: "Subscribe"}) Subscribe

mixin radio(obj)
  .form-check&attributes(attributes)
    input.form-check-input(
      name=obj.name
      type="radio"
      checked=obj.checked ? true : false
      disabled=obj.disabled ? true : false)
    span.form-check-label
      block
